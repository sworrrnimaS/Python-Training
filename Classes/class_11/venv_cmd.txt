# firstly download :
    #-->  python -m pip install --upgrade pip

#to upgrade pip :
    #---> python.exe -m pip install --upgrade pip

#to check the version :
    #---> python -m pip --version

#to download venv:
    #--> pip install virtualenv

#to create virtual environment:
    #--> python -m virtualenv venv
   
   #(then create .gitignore in the line of venv)
    #eti gareni venv activate bhako hudaina

   #to activate:
       #--> cd <envname>
       #--->  Scripts\activate
   #if activating doesnot work activate it by running Powershell as administrator and use following instruction
         #a)--->  Get-ExecutionPolicy  ; should get 'Restricted'
        #b)--->  Set-ExecutionPolicy RemoteSigned 
        #c)(then type 'Y' and proceed and it must be resolved)

#to install streamlit:
    #--> pip install streamlit

#venv bata baira aauna 'deactivate' lekhne
    



